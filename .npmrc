# Tell npm to install devDependencies even when NODE_ENV=production.
# This is required because Render sets NODE_ENV=production during the build phase,
# which would otherwise skip devDependencies like @tailwindcss/postcss and tailwindcss
# that are needed to compile globals.css at build time.
production=false
